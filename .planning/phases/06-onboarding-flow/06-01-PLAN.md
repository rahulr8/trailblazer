---
phase: 06-onboarding-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/onboarding/RotatingLogo.tsx
  - components/auth/OnboardingScreen.tsx
  - components/auth/LoginScreen.tsx
autonomous: true

must_haves:
  truths:
    - "Skip button in top-right corner of welcome pager bypasses to login"
    - "TB+ logo rotates continuously with slow compass-like animation on all onboarding screens"
    - "Terms & Conditions link on login screen opens in-app browser"
  artifacts:
    - path: "components/onboarding/RotatingLogo.tsx"
      provides: "Continuous rotation animation using Animated.loop"
      contains: "Animated.loop"
    - path: "components/auth/OnboardingScreen.tsx"
      provides: "Skip button in top-right corner"
      contains: "Skip"
    - path: "components/auth/LoginScreen.tsx"
      provides: "Tappable Terms & Conditions link opening in-app browser"
      contains: "WebBrowser"
  key_links:
    - from: "components/auth/OnboardingScreen.tsx"
      to: "onComplete callback"
      via: "Skip button onPress calls onComplete"
      pattern: "onComplete"
    - from: "components/auth/LoginScreen.tsx"
      to: "expo-web-browser"
      via: "Pressable wrapping Terms text"
      pattern: "openBrowserAsync"
---

<objective>
Enhance existing welcome pager and login screen with skip button, rotating logo animation, and Terms & Conditions link.

Purpose: Polish the first-impression screens users see — skip button respects user time, rotating logo adds visual polish, and T&C link meets legal requirements.
Output: Enhanced OnboardingScreen with skip button, animated RotatingLogo, and LoginScreen with tappable T&C link.
</objective>

<execution_context>
@/Users/turbolaunch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/turbolaunch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-onboarding-flow/06-CONTEXT.md
@.planning/phases/06-onboarding-flow/06-RESEARCH.md
@components/onboarding/RotatingLogo.tsx
@components/auth/OnboardingScreen.tsx
@components/auth/LoginScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add continuous rotation animation to RotatingLogo and skip button to OnboardingScreen</name>
  <files>
    components/onboarding/RotatingLogo.tsx
    components/auth/OnboardingScreen.tsx
  </files>
  <action>
**RotatingLogo.tsx** — Add continuous slow rotation animation:
- Import `{ useRef, useEffect }` from react, `{ Animated, Easing }` from react-native
- Replace `Image` with `Animated.Image` for the logo
- Create `rotateValue = useRef(new Animated.Value(0)).current`
- In useEffect, start `Animated.loop(Animated.timing(rotateValue, { toValue: 1, duration: 8000, useNativeDriver: true, easing: Easing.linear }))` — 8 seconds per rotation for slow compass-like effect
- Interpolate: `inputRange: [0, 1]`, `outputRange: ['0deg', '360deg']`
- Apply `transform: [{ rotate }]` to the Animated.Image style
- Keep the static glow View unchanged (glow does NOT rotate)

**OnboardingScreen.tsx** — Add skip button in top-right corner:
- Add a "Skip" Pressable positioned absolutely in the top-right area: `position: 'absolute', top: insets.top + Spacing.lg, right: Spacing['2xl'], zIndex: 10`
- Text content: "Skip" with `fontSize: 16, fontWeight: '600'`, color: `colors.textSecondary`
- onPress calls `onComplete?.()` (same as completing the last page — this sets the `@trailblazer_onboarding_seen` flag via the callback in _layout.tsx)
- Add `hitSlop={{ top: 10, bottom: 10, left: 20, right: 20 }}` for better tap target
- Support swipe gestures: Keep existing FlatList horizontal + pagingEnabled (already supports swipe — Claude's discretion decision)
  </action>
  <verify>
Run `bunx tsc --noEmit` — no type errors. Visually confirm:
1. RotatingLogo renders with continuous rotation
2. Skip button appears top-right on welcome screens
3. Skip button triggers onComplete callback
  </verify>
  <done>
RotatingLogo continuously rotates (8s per revolution, native driver). OnboardingScreen shows "Skip" button in top-right that calls onComplete to bypass welcome pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Terms & Conditions in-app browser link to LoginScreen</name>
  <files>
    components/auth/LoginScreen.tsx
  </files>
  <action>
**LoginScreen.tsx** — Make Terms & Conditions text tappable with in-app browser:
- Import `* as WebBrowser from 'expo-web-browser'`
- Add handler: `const handleTermsPress = async () => { await WebBrowser.openBrowserAsync('https://trailblazer.com/terms', { presentationStyle: WebBrowser.WebBrowserPresentationStyle.AUTOMATIC }); };`
- Replace the static `<Text>Terms & Conditions Apply</Text>` with a `<Pressable onPress={handleTermsPress} hitSlop={{ top: 10, bottom: 10, left: 10, right: 10 }}>` wrapping the Text
- Change text color from `colors.textTertiary` to `colors.primary` to indicate it's tappable (visual affordance)
- Keep the same positioning at bottom of screen

Per user decision: Terms opens in-app web view modal (user stays in app). expo-web-browser is already installed.
  </action>
  <verify>
Run `bunx tsc --noEmit` — no type errors. Visually confirm Terms text is now tappable and colored as a link.
  </verify>
  <done>
"Terms & Conditions Apply" text on login screen is tappable (colored as link) and opens placeholder URL in in-app browser via expo-web-browser.
  </done>
</task>

</tasks>

<verification>
1. `bunx tsc --noEmit` passes with zero errors
2. RotatingLogo shows continuous rotation animation on welcome, login, and permission screens
3. Skip button visible in top-right of welcome screens, tapping it skips to login
4. Terms & Conditions text on login screen is visually a link and opens in-app browser on tap
5. Existing onboarding flow (swipe between pages, arrow button navigation) still works
</verification>

<success_criteria>
- RotatingLogo animates with 8-second continuous rotation using native driver
- Skip button on OnboardingScreen calls onComplete to bypass welcome pages
- Terms & Conditions on LoginScreen opens in-app browser to placeholder URL
- No type errors, no regressions to existing onboarding/login flow
</success_criteria>

<output>
After completion, create `.planning/phases/06-onboarding-flow/06-01-SUMMARY.md`
</output>
